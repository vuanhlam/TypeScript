const role = [2, 'author'];

role.push('admin')
role[1] = 1

console.log(role)


// Tuples Type
//* 1. fixed element
//* 2. exact type
let rolePromax: [number, string];

rolePromax = 1
rolePromax = [1, 2]
rolePromax= 'ahihi'
rolePromax = [1, 'ahihi']

