const sum = (a: number, b:number): number => {
    return a + b;
}

const handleLogs = (message: string): void => {
    console.log('message: ', message)
}

//* void đa phần làm với function ít khi dùng với biến
//* void báo hiệu với function không trả về giá trị 